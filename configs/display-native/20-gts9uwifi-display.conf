[Seat:*]
# Run after Xorg is ready but before Slick Greeter.  A systemd ExecStartPost
# raced the greeter on cold boot, so it sometimes laid itself out against the
# connectorless primary's temporary geometry and clipped the embedded Onboard.
display-setup-script=/usr/libexec/gts9uwifi-panel-reinit
