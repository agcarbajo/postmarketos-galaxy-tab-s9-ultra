# Temporary bring-up diagnostics: log the QMI conversation, boot/board
# selection and WMI traffic in the journal (WMI|BOOT|QMI = 0x62).  Remove
# once wlan0 is stable.
options ath12k debug_mask=0x62
